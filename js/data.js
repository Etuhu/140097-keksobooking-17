'use strict';

(function () {
  // // Проверяет полноту полученных с сервера данных
  // var checkCompletenessOfData = function (dataLength, element) {
  //   if (dataLength !== 0) {
  //     cardElement.querySelector(element).hidden = true;
  //   } else {
  //     cardElement.querySelector(element).hidden = false;
  //   }
  //   return element;
  // };
  //
  //
  // var insertTextContent = function (parentEl, childEl, fieldValue) {
  //   parentEl.querySelector(childEl).textContent = fieldValue;
  // };


})();
